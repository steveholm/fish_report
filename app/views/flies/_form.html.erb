<%= form_for(@fly) do |f| %>
  <% if @fly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fly.errors.count, "error") %> prohibited this fly from being saved:</h2>

      <ul>
      <% @fly.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br />
    <%= f.number_field :location_id %>
  </div>
  <div class="field">
    <%= f.label :picture_url %><br />
    <%= f.text_field :picture_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
